# Runtime secrets must remain local; the tracked template is the only exception.
.env
.env.*
!.env.example

# Dependency and generated build artifacts.
node_modules/
coverage/
dist/
build/
.next/
.turbo/
# Vite build output for the dashboard SPA (generated at build/deploy time).
apps/web/public/

# Claude Code managed worktrees
.claude/
.omc/
.omo/
.omx/
.deps/
.codegraph/
.artifacts/
