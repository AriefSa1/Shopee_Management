import { createCopyPreview, createStableCopyIntent } from "./copy-preview.ts"
import { OrganizationIdSchema, ShopIdSchema } from "./model.ts"
import { PostgresCopyPreviewRepository } from "./postgres-copy-preview.ts"
import { FakeCopyPreviewPostgresExecutor } from "./postgres-copy-preview.fake.ts"
const organizationId=OrganizationIdSchema.parse("10000000-0000-4000-8000-000000000001"); const shopId=ShopIdSchema.parse("30000000-0000-4000-8000-000000000001"); const preview=createCopyPreview({ source:{ organizationId, sourceShopId:shopId, sourceProductId:"p-1", capturedAt:"2026-09-09T00:00:00.000Z", completeness:"complete", title:"Product", description:"Description", categoryId:"cat", attributes:{color:"red"}}, requirement:{organizationId,destinationShopId:shopId,capturedAt:"2026-09-09T00:00:00.000Z",completeness:"complete",allowedCategoryIds:["cat"],requiredAttributes:["color"],maximumTitleLength:100},mapping:{destinationCategoryId:"cat"}}); const intent=createStableCopyIntent(preview); const repo=new PostgresCopyPreviewRepository(new FakeCopyPreviewPostgresExecutor()); await repo.save({intent,preview}); const read=await repo.read({organizationId,destinationShopId:shopId},intent.copyIntentId); console.log(JSON.stringify({scenario:"copy-preview-postgres-persistence-contract",saveRead:read?.intent.copyIntentId===intent.copyIntentId,previewImmutable:read?.preview.previewHash===preview.previewHash,preConfirmationShopeeMutationTotal:0,networkCalls:0,databaseCalls:0,kmsCalls:0,shopeeMutations:0,externalWrites:0,secretFieldsPresent:false,livePostgres:"not_run"}))
